# Buscador de Repos de GitHub

Una aplicación de escritorio en Python (con Tkinter) que se comunica con la API de GitHub para buscar los repos de un usuario.

## Uso:

1. Instala las dependencias:

```bash
pip install -r requirements.txt